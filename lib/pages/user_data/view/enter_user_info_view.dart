

import 'package:flutter/material.dart';

class EnterUserInfoView extends StatelessWidget {
  const EnterUserInfoView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
